<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme, for dark theme. -->
    <style name="Theme.LineClearance" parent="Base.Theme.LineClearance">
        <!-- In dark mode, we can use a slightly different primary color if needed -->
        <item name="colorPrimary">@color/pgvcl_primary_dark</item>
        <item name="colorPrimaryVariant">@color/pgvcl_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>
</resources>